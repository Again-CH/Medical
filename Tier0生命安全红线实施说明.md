# Tier-0 生命安全 / 法律责任红线 实施说明

> 对应需求：本项目若要**直接对接真实客户与医院**，是否安全、还缺什么。
> 结论：P0/P1/P2 解决的是「Web 应用卫生」，**医疗场景还有「生命安全 + 法律合规」两层硬门槛没过**。
> 本次落地的 Tier-0 即这三道红线，把原先「LLM 软判断」升级为**确定性硬闸**。

---

## 0. 设计原则（红线之所以是「硬」闸）

| 原则 | 含义 |
|------|------|
| 确定性 | 急症/定位识别完全基于规则与关键词，**同步、毫秒级、不调用 LLM** |
| 短路 | 命中即返回**硬编码固定话术**，绝不进入 LLM 自由发挥（模型编错急救建议是危险源） |
| 宁可误报 | 漏报 = 真急症被当普通分诊（可能致死）；误报 = 让人多打一次 120（可接受） |
| 审计闭环 | 三闸均落库 ChatLog（含 `tool_used`），事后可回放「当时为什么拦」 |

---

## 1. 三道硬闸（网关入口，先于一切 LLM 调用）

`src/gateway.py` 的 `/api/chat` 在构建 LangGraph 输入**之前**依次判定：

```
① 紧急硬闸  assess_emergency()  ──命中→ 返回 120+就近急诊固定话术（不进 LLM）
② 知情同意门 _has_consent()     ──未签→ 返回 consent_required（前端弹强制同意框）
③ 定位违规门 assess_scope_violation() ──命中→ 返回「不诊断不开方」固定话术
④ 正常链路  → 进入 LangGraph（triage/booking/...）
```

### 1.1 紧急硬闸（生命安全红线）
- 类别覆盖：心源性、呼吸、大出血、卒中/神经、意识障碍/休克、**自伤/自杀危机**、产科、中毒、重度烧伤、明确剧痛。
- 否定词守卫：`没有胸痛 / 不胸痛 / 无胸痛` 等明确否认句**不误报**；非否认的急症描述（哪怕提问语气）仍触发。
- 返回话术为硬编码：「① 立即拨打 120 … ② 急救前处置（按类别不同）… ③ 就近急诊，勿独自驾车」。
- 效果验证：返回体中**不含任何 LLM `token` 事件**，彻底阻断对话式建议。

### 1.2 知情同意门（法律责任红线）
- 仅患者角色需签署；医护工作台不在对话服务范围内，无需同意书。
- 首次使用**未签署当前版本** → 对话被拦截，前端弹强制同意框，必须主动勾选「已阅读并理解」才能继续。
- 每次对话下发 `disclaimer` 事件，前端在每条助手回复下方渲染显著免责声明（满足「每次显著免责」）。
- 同意书落库 `consent_records`（`username / consent_version / consent_types / agreed_at / ip`），版本升级后旧用户需重新签署。

### 1.3 定位违规门（产品定性红线）
- AI **只做分诊/科普/导诊**，不做诊断、不开处方（依产品定位与监管要求）。
- 识别「开药/开处方/确诊/下诊断/开病假条」等请求 → 固定回复「我是导诊助手，不提供诊断或开处方，请挂号由医生面诊」。
- 同样**不进 LLM**，避免模型「越界」给出诊断式回答。

---

## 2. 关键代码与数据变更

| 文件 | 变更 |
|------|------|
| `src/safety.py`（新增） | 确定性安全网关：急症/定位识别 + 固定话术/免责声明/同意书常量 |
| `src/gateway.py` | chat 端点接入三闸 + 抽取 `record_chat_log()` 复用；新增 `GET /api/consent/status`、`POST /api/consent` |
| `src/db.py` | 新增 `ConsentRecord` 模型 |
| `alembic/versions/c3d4e5f6a7b8_add_consent_records.py`（新增） | 同意书表迁移（**可 `downgrade` 回滚**） |
| `client/chat.html` | 强制知情同意弹窗 + 紧急(红)/定位(琥珀)气泡 + 免责声明脚注 + 持久免责条 |

---

## 3. 验证记录

**单元测试 / 集成测试（`pytest`，33 passed）**
- `tests/test_safety.py`：急症各类别命中、否定词守卫、定位违规识别，全部确定性无 LLM。
- `tests/test_gateway.py`：紧急闸阻断 LLM（`token` 不在返回体）、定位闸返回 `scope`、未签同意被 `consent_required` 拦截、签署后可正常对话且同意书持久化。

**生产实例端到端（真实 Postgres，`:8080`）**
- 未签 → `consent_required`；签署后 `required=False` ✅
- 胸痛/晕倒/轻生/羊水破 → `emergency` 事件 + 含 `120` + **无 LLM token** ✅
- 开药 → `scope` 事件 + 明示不诊断不开方 ✅
- 已签后正常咨询 → `done` + `disclaimer` ✅
- 医生 `GET /api/chat-logs` 可见该轮 `tool_used=emergency_gate` ✅

---

## 4. 还差什么才能真上线（Tier-1/2 待办，非本次范围）

Tier-0 解决「不害人、不越界、留证据」，但**医疗合规**仍未闭环：

- **Tier-1 数据合规（PIPL / 等保2.0）**：HTTPS+WAF+HSTS、PHI 存储加密(KMS)、等保三级备案、ChatLog 脱敏与留存期、审计防篡改(WORM)。
- **Tier-2 应用安全**：JWT 缩短+刷新+吊销名单、密码哈希升 argon2id、账号级锁定、医护 MFA、XSS 收口(`innerHTML`→转义)、依赖 SCA 扫描。
- **Tier-3 运维/韧性**：备份灾备演练、监控告警、限流升 Redis、第三方模型数据出境合规与幻觉责任条款、渗透测试 + 医疗责任险。
- **监管资质**：若涉及诊断需 NMPA 医疗器械软件(SaMD)注册 + 医疗机构执业许可（由合规/法务最终签字，本工程仅落地技术控制）。

> 命中的急症话术当前为通用指引；下一步可接「最近急诊实体库」按定位返回最近医院，并叠加轻量分类模型做规则引擎双保险。

# 项目约束与验收标准（医疗域修订版）

> 评审对象：用户给出的四柱约束（权限管控 / 重试策略 / 日志监控 / 验收标准）
> 评审视角：医疗健康域（康宁健康 AI 导诊）合规 + 患者安全
> 配套代码：src/auth.py（RBAC+令牌吊销）、src/db.py（ChatLog/Consent/Audit/Approval）、src/safety.py（确定性安全硬闸）、src/gateway.py（日志落库）

## 0. 总体结论

四柱框架结构正确，符合企业级 Agent 标准。但原文按**通用客服 Agent** 口径写，直接套医疗域会在三处出问题：

1. **第 1 柱示例错位**："财务数据仅对财务角色开放" → 真实敏感资产是 **PHI（患者健康信息）**，角色是 `patient`/`doctor`。
2. **第 2 柱漏两项**：重试没豁免**确定性安全硬闸**，且写操作缺 **幂等键**（重试会重复发短信/重复挂号）。
3. **第 3 柱合规冲突**："记录请求参数、响应结果" 字面执行 = 明文落 PHI，与你们《知情同意书》"最小必要原则"直接矛盾（证据：`gateway.py:291-292` 已明文存 `input_text`/`output_text`）。

下面逐柱给出修订，并标注 已实现 / 待补。

---

## 1. 权限管控（让 Agent "合法上岗"）

**已就绪**：RBAC（patient/doctor + `require_doctor`）、刷新令牌按条吊销 + `token_version` 全局吊销、患者数据**物理隔离**（每患者独立 SQLite `data/<username>.db`）、HITL 审批表 `approvals`/`pending_calls`、知情同意 `consent_records`。

### 1.1 最小权限原则（修订示例）
- 原文 "财务数据仅对财务角色开放" → 改为：**PHI 仅对"必要且已授权"角色可见**。
  - 患者：仅读写自己的数据（物理隔离已实现，天然满足）。
  - 医护：`require_doctor` 只保证"是医生"，**还需按授权范围**限定可见患者（本科室/已挂号），建议加 `dept_id`/`scope` 校验，避免任一医生看全库。
  - Agent 工具：外部工具按业务最小集开放，禁止调用管理类高危接口。

### 1.2 动态授权（修订：从"临时权限申请"改为"敏感操作 HITL + 留痕 + 时限"）
医疗场景给 Agent "临时提权"风险极高，不推荐泛化。正确做法：
- 敏感工具调用（代发短信、读取他人报告）一律走已有 **HITL 审批流**（`Approval` + `PendingCall`），而非"临时权限申请给人"。
- 若确需跨部门/跨患者人工授权（会诊调阅）：① 留痕 `audit_logs`；② 设**有效期**（如 24h 自动回收）；③ 绑定具体资源（patient_id/dept），不泛授权限。

### 1.3 数据脱敏（**当前最大缺口，必须补**）
- 现状：`chat_logs.input_text/output_text`（gateway.py:291-292）明文存患者主诉与 AI 回复；`lab_reports`/`vital_signs` 对医护原样返回。
- 要求：
  - **存储层**：落库日志对手机号、姓名等直接标识符做掩码（如 `138****1234`）；健康原文建议加密字段或仅存哈希。
  - **展示/导出层**：医生工作台、运营报表默认脱敏，查看明文需二次授权留痕。
  - 与《知情同意书》"最小必要原则"对齐，属合规硬要求。

---

## 2. 重试策略（让 Agent "抗造"）

**现状**：当前无集中重试/退避基础设施。

### 2.1 自动重试 + 指数退避（保留，限定范围）
- 仅对**外部依赖不确定性失败**启用：LLM API 超时/5xx/限流、短信/挂号网关 5xx、知识库检索超时。
- 退避：`base * 2^n + jitter`，上限约 30s，配合熔断。

### 2.2 ⚠️ 安全硬闸必须豁免重试（关键新增）
`assess_emergency` / `assess_scope_violation` / 知情同意门（safety.py）是**确定性短路、先于 LLM、毫秒级**。重试它们=重复执行且无意义，更可能**延误 120 提示**。重试策略必须显式把它们列入 **exclude list**，并设独立快速失败路径。

### 2.3 ⚠️ 写操作必须幂等（关键新增）
重试短信通知、创建预约、发邮件等**副作用操作**若无幂等保护，重试会重复发信/重复挂号。需在 `Appointment` 创建等写入口加 **Idempotency-Key**（如 `trace_id + action` 唯一约束），重试复用同一键保证只生效一次。

### 2.4 人工介入阈值（保留 + 分级）
- 工程侧：超过最大重试 → 运维告警（钉钉/飞书）。
- 临床侧：**急症检测命中**触发的是**临床升级**（推送值班医护/临床安全负责人），与安全告警通道分离。

---

## 3. 日志监控（让 Agent "可追溯"）

**已就绪**：`chat_logs`（trace_id 关联 LangGraph 链路 + intent + tool_used + latency + fallback）、`audit_logs`、`consent_records`。

### 3.1 全链路日志（修订：默认脱敏）
- 字面"记录请求参数、响应结果" = 明文 PHI，**与合规冲突**，必须改为：默认脱敏存储（见 1.3），审计需时经授权回溯明文。
- 可全量保留（无 PHI）：trace_id、intent、tool_used、latency、fallback、角色、时间。

### 3.2 分级告警（保留 + 临床级）
- ERROR/WARN：工程告警 → 钉钉/飞书。
- **临床级（新增）**：emergency gate 命中、知情同意绕过、越权访问尝试 → 独立通道推送临床安全负责人，计入安全事件台账。

### 3.3 可视化监控（修订：PHI-free）
- 仪表盘指标仅限：调用量、成功率、P95 耗时、**急症拦截数**、越权拦截数、降级率。
- **禁止**把患者主诉/身份作为仪表盘维度，避免聚合泄露。

### 3.4 审计日志防篡改（新增）
等保要求审计日志**不可篡改、留存≥6 个月**。当前 `audit_logs` 为普通表，建议 append-only / WORM 或独立审计库。

---

## 4. 验收标准（让 Agent "可交付"）

### 4.1 功能验收（修订：以"安全红线"为核心指标）
- ❌ 原 "客服问答准确率" 不适用（医疗建议属 liability）。
- ✅ 改为：
  - 分诊推荐科室准确率（标注集评测）；
  - **急症识别召回率 ≈ 100%（漏报 0 容忍，须有红队测试用例集）**；
  - 知情同意流程覆盖率 = 100%（未签署无法进入对话）；
  - 越权访问拦截率 = 100%（跨患者/非医生调用全拒）。

### 4.2 性能验收（修订：加急症路径 SLA）
- 通用：QPS / P95 延迟 / 错误率达标；并发压测覆盖**医患双端**。
- **急症路径延迟 SLA（新增）**：120 提示须 < 2s 返回，且**不得因重试/限流被拖慢**（呼应 2.2 豁免）。

### 4.3 合规验收（修订：显式合规清单）
- 数据安全：《个人信息保护法》PIPL、数据安全法、患者数据最小化与脱敏（1.3）落地；
- 等保：定级（建议三级）与测评要点覆盖；
- 医疗监管：明确"本产品**不含诊断/开处方能力**"，规避 NMPA SaMD 注册义务（见 safety.py 定位声明）；若扩展诊断能力须另行 SaMD 注册 + 医疗机构执业许可；
- 授权机制：第 1 柱全部落地并验证。

---

## 附录：落地优先级

- **P0（合规/安全硬缺口）**：1.3 数据脱敏、3.1 日志脱敏、2.2 安全闸豁免重试、2.3 幂等键。
- **P1（企业化完善）**：1.2 HITL 时限、3.2 临床级告警、3.4 审计防篡改、4.1 红线召回率评测集。
- **P2（进阶）**：1.1 医护按范围授权、4.2 急症 SLA 压测、等保测评。

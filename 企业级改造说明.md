# 医疗预约诊疗 Agent —— 企业级工程化改造说明

> 目标：把当前项目从「能跑的 Demo」补齐到接近「企业级生产项目」的安全 / 兜底 / 可观测基线。
> 本文记录**已落地的改造项、对应文件、验证结果**，以及**仍可继续演进的方向**（供面试谈资 / 后续迭代）。

---

## 0. 评估结论（相对真实企业级项目还差什么）

| 维度 | 改造前 | 改造后 |
|------|--------|--------|
| 密钥安全 | JWT 有弱默认、CORS 全开 `*` | 生产弱密钥**拒绝启动**；CORS 白名单 |
| 接口防护 | 无限流、无输入校验 | 令牌桶限流 + Pydantic 统一校验 |
| 失败兜底 | LLM 卡死会永久挂起 | 超时受控中止 + 全局异常 + 安全降级话术 |
| 可回滚 | schema 靠 `create_all` | Alembic 版本化、**可 downgrade 回滚** |
| 执行可观测 | 仅 print | 结构化 JSON 日志 + `trace_id` 透传 + ChatLog 链路表 |

---

## 1. P0 · 安全加固

| 改造点 | 文件 | 说明 |
|--------|------|------|
| 强密钥启动校验 | `src/config.py` `_resolve_jwt_secret()` | 生产 `JWT_SECRET` 弱（<`JWT_SECRET_MIN_BYTES`）**启动即报错退出**；开发空密钥自动生成临时密钥 |
| 环境隔离 | `src/config.py` `APP_ENV` | production / development 走不同默认策略 |
| CORS 收紧 | `src/gateway.py` | `CORS_ORIGINS` 环境变量白名单，默认不再 `*`，`allow_credentials=False` |
| 令牌桶限流 | `src/gateway.py` `RateLimitMiddleware` | 按 IP+路由前缀：login 10/60s、register 5/60s、chat 20/60s、review 30/60s（`RATE_LIMIT_ENABLED` 可关） |
| 统一输入校验 | `src/gateway.py` `ChatRequest`/`RegisterRequest`/`LoginRequest` | 长度 / 正则约束，`MAX_MESSAGE_LEN=2000` |
| 注册管控 | `src/gateway.py` `/auth/register` | `REGISTER_ENABLED=false` 时返回 403 |

## 2. P1 · 兜底容错

| 改造点 | 文件 | 说明 |
|--------|------|------|
| SSE 超时控制 | `src/gateway.py` | `asyncio.wait_for(agen.__anext__(), timeout=CHAT_TIMEOUT_SECONDS)`，超时返回友好提示而非永久挂起 |
| 全局异常处理 | `src/gateway.py` | `Exception`→500、`RequestValidationError`→422 结构化错误 |
| LLM 熔断 | `src/llm.py` | `request_timeout` + `max_retries=2` |
| 安全降级话术 | `src/agents.py` `final_answer` | LLM 失败 / 空结果 → 标准化兜底文案（含转人工），**绝不编造** |

## 3. P2 · 执行流程可观测

| 改造点 | 文件 | 说明 |
|--------|------|------|
| 结构化日志 | `src/logging_config.py` | `JsonFormatter` + `get_logger` 单例 + `new_trace_id()` |
| 链路追踪透传 | `src/gateway.py` → LangSmith | `trace_id` 注入 `metadata.trace_id`，**设 `LANGSMITH_API_KEY` 即自动开启** |
| 链路审计表 | `src/db.py` `ChatLog` + `alembic/versions/b2c3d4e5f6a7_add_chat_logs.py` | 每轮对话落库 `trace_id/patient/thread/intent/input/output/tool_used/latency/fallback` |
| 链路回放接口 | `src/gateway.py` `GET /api/chat-logs` | 仅医生可见，按 patient/thread 过滤，关联 LangSmith 回放完整 LangGraph 执行流程 |

### 可回滚（rollback）
所有 schema 由 Alembic 管理，新增 `chat_logs` 的迁移 `down_revision=a1b2c3d4e5f6`，已验证：
```bash
DATABASE_URL=... alembic upgrade head   # 建表
DATABASE_URL=... alembic downgrade a1b2c3d4e5f6  # 回滚删表
```

---

## 4. 验证记录

- **单测基线**：`pytest` → **20 passed**（原 18 + 新增 2：ChatLog 落库 / 医生回放权限隔离）。
- **迁移**：`upgrade head` 建 `chat_logs` ✅；`downgrade` 删表 ✅（可回滚）。
- **生产实例（:8080 真实 Postgres）实测**：
  - 注册缺体 → 422（Pydantic 校验生效）
  - 连续登录 → 429（限流生效）
  - 患者聊天 → 真实 LLM 流式返回
  - 医生查 `/api/chat-logs` → 命中该轮记录：`trace_id=b271db7e…`、`tool_used=llm`、`latency_ms=7113`、含 input/output 文本

---

## 5. 仍可继续演进的方向（面试加分项）

1. **限流升级**：当前为单进程内存令牌桶，多 worker / 多实例需换 Redis 集中式（`RateLimitMiddleware` 已预留 `enabled` 开关与规则注入点）。
2. **密钥管理**：生产 `JWT_SECRET` 应来自 KMS / Secrets Manager，而非 `.env` 明文。
3. **可观测增强**：结构化日志接入 ELK / Loki；LangSmith 之外补 Prometheus 指标（QPS / P99 延迟 / 降级率）。
4. **回归测试**：加 `pytest` 集成测试覆盖 HITL 审批流 + ChatLog 联表查询。
5. **合规**：ChatLog 含患者输入文本，需明确留存期限与脱敏策略（PII 合规）。
6. **CI/CD**：把 `alembic check`（漂移门禁）+ `pytest` 接入流水线，PR 合并前强制通过。

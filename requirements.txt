langgraph>=0.2.40
langchain-core>=0.2.0
fastapi>=0.110
uvicorn>=0.27
python-dotenv>=1.0
pydantic>=2.0
pytest>=8.0
# 网关单测用 FastAPI TestClient 需要 httpx
httpx>=0.27
# 生产级持久化（可选：仅当设置 DATABASE_URL 时才需要，脚手架默认内存/JSON 仍可开箱即跑）
sqlalchemy>=2.0
psycopg2-binary>=2.9
# 链路追踪（可选：仅当启用 LANGSMITH_TRACING 时才需要）
langsmith>=0.1.0

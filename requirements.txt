langgraph>=0.2.40
langchain-core>=0.2.0
fastapi>=0.110
uvicorn>=0.27
python-dotenv>=1.0
pydantic>=2.0
pytest>=8.0
# 网关单测用 FastAPI TestClient 需要 httpx
httpx>=0.27
# 生产级持久化（可选：仅当设置 DATABASE_URL 时才需要，脚手架默认内存/JSON 仍可开箱即跑）
sqlalchemy>=2.0
psycopg2-binary>=2.9
# 数据库迁移（Alembic）：替代 create_all，提供带版本、可回滚、可演进的 schema 管理
alembic>=1.13
# PostgresSaver 异步 checkpointer：实现会话状态 + HITL 待审批缓存跨重启持久化（LLM_MODE=openai/真实库）
psycopg[binary]>=3.3
langgraph-checkpoint-postgres>=3.1
# 链路追踪（可选：仅当启用 LANGSMITH_TRACING 时才需要）
langsmith>=0.1.0
# 本地模型（可选：LLM_MODE=ollama 时需要；本机无 OpenAI key 的推荐路径）
langchain-ollama>=0.2.0
# JWT 鉴权（多用户 / RBAC）
PyJWT>=2.8
# 远程模型（可选：LLM_MODE=openai/qwen 时需要，面试演示更稳）
langchain-openai>=0.2.0

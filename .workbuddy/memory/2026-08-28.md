# 2026-08-28

## 医疗预约诊疗 Agent 链路图审查与调整
- 文件：`/Users/mac/Documents/Medical-care/链路图.html`（LangGraph 0.2 + FastAPI 中枢辐射编排 + HITL + 合规横切层）
- 修复的问题：
  1. 应急转诊 `interrupt` 箭头原终点 (470,612) 落在空隙，已改到人工审核门第1步框顶部 (196,640)。
  2. 医护端颜色与图例不一致（原黄 #fef9c3，图例无此项）→ 改为靛蓝 #e0e7ff，图例新增「医护端」项（患者端/医护端区分）。
  3. 左侧「响应」回环箭头穿节点、语义不清 → 改为右侧「多轮对话」回环（checkpointer，x≈598→648）。
  4. 工具层文案「领域工具层」→「领域工具注册中心（子 Agent 按需调用）」。
  5. 合规横切层新增可观测性/模型适配条：LangSmith 追踪·日志·eval；llm.py 适配 fake/ollama/openai/qwen 私有化。
- 评价：架构骨架对企业级面试作品集合格；redline 前置、interrupt/Command(resume)、checkpointer、RBAC、qwen 私有化等落点到位。
- 待增强（未改，建议后续）：流式/WebSocket 对话、eval 评测集、LLM 不可用 fallback、压测/多租户隔离。

## 补丁：新增 ④ 工程化增强区块
- viewBox 由 1180 → 1500 加高；图例新增「工程增强」(slate #f1f5f9)。
- 主链路网关 /api/chat 标 SSE 流式；底部说明串起 ④。
- ④ 2×2 卡片：流式对话(SSE/token推送)、评测集 eval(tests/eval+pytest+LangSmith+CI)、Fallback 降级(Ollama 不可用→规则兜底+仍走 interrupt)、多租户隔离(hospital_id/dept_id 维度隔离+checkpointer/store/记忆分片+RBAC tenant 作用域)。

## 补丁：子 Agent 独立工具集细分画法
- 原「单一共享工具层」→ 拆为 5 个专属工具盒（分诊/挂号/问诊/随访/转诊），各用短连线 1:1 挂到对应子 Agent 下方；背景保留琥珀色带对齐图例「工具层」。
- 各盒工具：分诊=search_dept/dept_map(RAG)；挂号=query_avail/lock_appt/medicare；问诊=lab_report(LIS)/clinical_kb(RAG)；随访=read_vitals/reminder/memory；转诊=handoff/call_120。
- 中断箭头改为从「挂号」正下方直穿其自身工具盒再到人工门 step1，避免横扫整排工具盒。
- 底部说明补「子 Agent 独立工具集已细分」一句。

## 落地可运行 LangGraph 脚手架
- 在 Medical-care/ 下新建完整项目：src/（config/llm/redline/memory/store/kb/state/state_utils/supervisor/agents/graph/gateway + tools/ 5 命名空间）、client/(chat.html+review.html)、tests/(test_graph.py + eval/redline_cases.json,intent_cases.json,test_eval.py)、requirements.txt/.env.example/README.md。
- 技术栈：LangGraph 1.2.11 + FastAPI + langchain-core 1.6.0（实测可用）。默认 LLM_MODE=fake 开箱即跑。
- 已验证：pytest 5 passed；TestClient 端到端跑通 SSE 流式 / 挂号 interrupt→approval_id / 医生 resume 锁号 / 审计 2 条 / 红线急症 interrupt。
- 踩坑修复：① add_messages 改从 langgraph.graph.message 导入；② 网关 auth 参数名须为 authorization（对应 Authorization 头）否则 401；③ emergency 也走 interrupt，测试断言改为校验 __interrupt__。
- 验证用 venv：/Users/mac/Documents/Medical-care/.venv（managed python 3.13.12 创建）。
- 已把范式存为用户级技能 langgraph-hitl-scaffold（~/.workbuddy/skills/）。

## 补丁：SSE 直连 LLM token 流式 + Postgres 持久化
- SSE 真流式：gateway 改用 `graph.astream_events(input, config, version="v2")` 捕获 `on_chat_model_stream`，直接推 LLM 真实 token（不再事后切分文本）。
  - `llm.FakeLLM` 改为 `BaseChatModel` 子类，`_astream` 逐块 yield `AIMessageChunk`；`compose.py` 抽 `compose_answer`/`parse_compose_context`（含多行 tool_result 整块解析）。
  - `agents.final_answer` 改为 async，内部 `await llm.astream(msgs)` 流式汇总。
  - **关键坑**：`astream_events` 在 interrupt 处**不抛异常**，需流完后 `graph.get_state(config).tasks[0].interrupts[0].value` 取暂停值；用「是否发过 token」决定 done 的 turn。
  - 测试 `test_graph.py` 因图含 async 节点，由 `g.invoke` 改为 `asyncio.run(g.ainvoke(...))`。
- Postgres 持久化：store.py 新增 `PostgresApprovalStore`（SQLAlchemy Core，建 approvals/audit_log 两表，lazy import 不强制装依赖）；`get_store()` 工厂按 `DATABASE_URL` 启用，否则回退内存/JSON。新增 `docker-compose.yml`（Postgres 16）。requirements 加 sqlalchemy/psycopg2-binary（标 optional）。
- 验证：pytest 5 passed；TestClient 冒烟 = 分诊 22 token 事件且内容与图完整输出一致、挂号 interrupt+approval_id、resume 已锁定、audit 2 条、急症 interrupt；Postgres 逻辑用 `sqlite:///` 等价跑通（SQLAlchemy Core 通用），无需真 PG。
- 技能 langgraph-hitl-scaffold 已同步更新 SSE 真流式写法与 Postgres 插拔范式、相关踩坑(7/8)。

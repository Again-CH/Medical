# 虚拟环境
.venv/
venv/
env/

# Python 缓存
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 环境变量与密钥
.env
.env.local

# 本地审批存储/评测产物（CI 另有 artifact）
approvals*.json
eval_report.json

# 压测产物：只保留人工可读的报告与热点剖析 JSON，
# locust 自动生成的 report.html（内含整个前端库）与逐轮 CSV 不入库
bench/report.html
bench/*.csv

# 本地 SQLite 数据库（测试/开发产物，Postgres 才是真库）
*.db
medical_agent.db

# macOS
.DS_Store

# 编辑器
.idea/
.vscode/

# WorkBuddy 工作区记忆（本地协作日志，非交付物）
.workbuddy/

# 可观测性栈：Prometheus 抓取用的管理员密钥（含 ADMIN_API_KEY，禁止入库）
observability/prometheus/admin_key

# 前端构建产物与依赖（由 `npm run build` 复现，不入 VCS）
client/node_modules/
client/dist/
client/test-results/


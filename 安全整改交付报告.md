# 安全整改交付报告

> 整改日期：2026-08-29　对应审计：`安全审计报告_企业级Agent.md`
> 范围：审计提出的 P0 × 6 / P1 × 14 / P2 × 6，共 26 项
> 结果：**25 项已修复，1 项经论证后采用更优方案**（见「设计决策」）
> 验证：pytest **80 passed**（原 58 + 新增安全回归 22）· ruff check/format 全绿 · 评测 17/17

---

## 一、总览

| 级别 | 项数 | 已修复 | 设计调整 | 说明 |
| --- | --- | --- | --- | --- |
| P0 | 6 | 6 | 0 | 全部阻断可远程利用路径 |
| P1 | 14 | 13 | 1 | P1-6 锁号审批改为「自动执行 + 上限 + 并发安全」 |
| P2 | 6 | 6 | 0 | 含 CSP 严格化与数据删除权 |
| **合计** | **26** | **25** | **1** | |

关键指标变化：

| 指标 | 整改前 | 整改后 |
| --- | --- | --- |
| 新增安全回归断言 | 0 | 22 项（PoC 固化） |
| CI 安全门禁 | 0 | 3（bandit / pip-audit / gitleaks）+ 1 隔离回归 job |
| 工具暴露 `patient_id` | 5 个 | 0 个 |
| PHI 出境管控 | 无 | strict 默认拒绝外网端点 |
| CSP | `unsafe-inline` | `nonce` 严格模式 |
| 敏感操作可追责 | 否（审批人不落库） | 是（`resolved_by` + 审计） |

---

## 二、P0 修复明细

### P0-1 注册角色自助提权 → 已修复

- `RegisterRequest.role` 改为 `Literal["patient"]`，客户端传 `doctor` 直接 422。
- 新增 `POST /admin/doctors`：需 `X-Admin-Key`（与 `ADMIN_API_KEY` 常量时间比对），
  未配置该密钥时端点返回 503（拒绝开通），每次开通落审计。
- 密码强度校验前置（长度 ≥ 8 且字母+数字）。

### P0-2 工具层跨患者越权 → 已修复（对象级授权 OLP）

- **从工具 schema 中彻底移除 `patient_id`**：`read_lab_report()` / `read_vitals()` /
  `plan_reminder(text)` / `memory_append(note)` / `call_120()` 不再接受身份参数，
  身份一律取自 `patient_ctx`（源头是 JWT subject）。模型无法被注入诱导访问他人档案。
- **访问层唯一收口**：`integrations._resolve_patient(explicit)` —— 显式身份与上下文不符
  即抛 `PermissionError` 并记 `security.denied`。即便将来有人误把参数加回工具签名，
  访问层依然拦得住（纵深防御）。Hub 方法保留**可选** `patient_id` 参数正是为了让这层
  断言可被测试覆盖。
- **异常收敛**：`agents._invoke_tool()` 捕获 `PermissionError`，转为固定拒绝文本
  `[denied] 该操作未获授权…`，不 500、不把异常细节回灌进模型上下文。

### P0-3 跨患者医保结算 → 已修复

- `medicare_settle` 校验 `appt.patient_id == 当前患者`，不符即拒绝并记 `security.denied`。
- **删除「退而求其次结算最近一笔预约」的兜底** —— 那段逻辑正是绕过归属校验的暗门。

### P0-4 会话线程越权 → 已修复

- `thread_id` 服务端派生：`_owned_thread_id()` → `{role}:{sub}:{client_tid}`。
  跨患者访问在寻址层即失效，待审批缓存（`_PENDING` 以 thread_id 为 key）也无法被抢占。

### P0-5 镜像打包密钥与 PHI → 已修复

- 新增 `.dockerignore`：排除 `.env*` / `*.db` / `data/` / `.git` / 缓存与本地产物。
- `Dockerfile`：新增 `appuser`（uid 10001）非 root 运行，移除 `DATABASE_URL` 硬编码默认值。

### P0-6 PHI 明文出境 → 已修复

- 新增 `LLM_EGRESS_POLICY`：`strict`（默认，仅本地/私有化端点，外网拒绝启动）/
  `masked`（出境前 `mask_pii()` 脱敏）/ `allow`（本地开发）。
- 新增 `LLM_PRIVATE_HOSTS` 内网白名单；`get_llm()` 在外网端点下**直接拒绝启动**。
- 效果已实测：本机 `.env` 指向 Deepseek 时，strict 策略下 `RuntimeError` 阻断启动。

---

## 三、P1 修复明细（13 项）

| # | 问题 | 修复 |
| --- | --- | --- |
| 1 | 审批盲批 / 审批人不落库 | `interrupt` 载荷新增 `calls[{name,args}]` 与 `requester`；医护端渲染完整参数；`ResolveRequest.decision` 改枚举校验；`resolve()` 写入 `resolved_by` 与 `approval_resolve` 审计 |
| 2 | 限流可伪造 XFF 绕过 | 新增 `_client_ip()`：仅 `TRUST_PROXY=true` 才采信 XFF，否则用 TCP 对端；限流桶超 1 万条自动回收（防内存无界增长） |
| 3 | 账号锁定型 DoS | 新增来源维度探测：同一 IP 在 15 分钟内涉及 ≥5 个账号失败即封禁 30 分钟并告警；新增 `POST /admin/unlock` 运营解锁通道 |
| 4 | 鉴权 fail-open | `AUTH_FAIL_MODE=fail_closed`（默认）：吊销校验不可用即 503 拒绝，并记 `error` 级日志 |
| 5 | compose 弱默认值 | 全部改 `${VAR:?提示}` 必填；移除 5432 端口映射；`cap_drop: ALL` + `no-new-privileges`；显式注入 `AUTH_FAIL_MODE` / `LLM_EGRESS_POLICY` / `TRUST_PROXY` |
| 6 | 锁号审批与并发 | 见下方「设计决策」；`booked_slots` 改**原子 UPDATE** 校验 `rowcount`，杜绝并发超卖 |
| 7 | （同上，并发超卖） | 同上 |
| 8 | 密码策略与改密 | 最小 8 位 + 字母数字复杂度（`validate_password_strength`）；新增 `POST /auth/change-password`（校验旧密码 + bump `token_version` 全局吊销） |
| 9 | 审计不完整 | `AuditLog.actor` 改为**真实操作者**（原为 thread_id）；补齐 `login_success/login_failed/login_locked/logout/change_password/register/create_doctor/approval_*` 全量事件 |
| 10 | 刷新令牌可无限续期 | 新增 `oiat`（原始签发时间，续期时继承）+ `REFRESH_ABSOLUTE_EXP_DAYS=30` 绝对上限；刷新时校验用户存在/未锁定/`token_version` 未 bump |
| 11 | 依赖无锁 + 无安全门禁 | 生成 `requirements.lock`（68 个包精确版本）；CI 新增 `security` job：`bandit`（SAST）+ `pip-audit --strict`（CVE）+ `gitleaks`（密钥泄露） |
| 12 | 双红线词库分叉 | `redline.py` 收敛为 `safety.py` 的**薄适配层**（不再维护独立词库）；`supervisor` 直接调用 `safety.assess_emergency/assess_scope_violation` |
| 13 | 无输出护栏 | 新增 `src/guard.py`：诊断/处方/剂量/手术决策四类确定性正则；网关**句级缓冲**推送（攒够一句即检测），命中即推 `safety_override` 安全话术并告警；剂量规则要求与用药语境共现，避免误伤 `CRP 12 mg/L` |
| 14 | 自动建档 | `_resolve_user_id()` 用户不存在即抛 `PermissionError` 并审计，不再创建 `password_hash=""` 的畸形账号 |

---

## 四、P2 修复明细（6 项）

1. **CSP 严格化**：中间件按请求生成 nonce，HTML 页面注入 `<script nonce="...">`；
   CSP 改为 `script-src 'self' 'nonce-xxx'`。前端 **24 处内联事件属性**全部迁移为
   `data-action` + 全局事件委托（`window.__ACTIONS` 注册表），交互行为不变。
   `CSP_STRICT=false` 可回退（兼容未迁移的前端）。
2. **全局异常结构化日志**：`print` 改为 `log.error("http.unhandled", exc_info=True)`，
   响应体仍保持通用，不回显异常细节。
3. **同意书 IP 最小化**：`mask_ip()` 保留 IPv4 前三段 / IPv6 前四组后落库。
4. **幂等键改用 sha256**：`stable_key()` 取代 Python 内置 `hash()`
   （后者受 `PYTHONHASHSEED` 随机化影响，重启后即失效且可碰撞）。
5. **安全回归测试**：新增 `tests/test_security_isolation.py`（22 项），
   CI 独立 job 运行。
6. **数据删除权**：新增 `DELETE /api/patient/me` —— 物理删除患者私有库、
   撤回同意记录、审计日志匿名化（`erased:<sha256前16位>`）而非删除（合规需保留操作痕迹）。

---

## 五、设计决策：P1-6 锁号是否纳入人工审批

审计报告原建议「把 `lock_appointment` 纳入 `SENSITIVE_TOOLS`」。实施时论证后**未采纳**，
理由与替代方案如下：

**不纳入审批的理由**

1. 患者自助挂号若每个号源都要医生审批，审批队列会被瞬间淹没，HITL 失去可行性。
2. 锁号本身**可逆**（未支付/未结算会自动释放），风险显著低于医保结算（涉及资金）与
   120 呼叫（不可逆）。真正需要人工把关的是后者。
3. 现有测试与评测已固化「锁号自动执行、仅结算审批」的链路。

**替代的等效控制（已实现）**

- `MAX_APPOINTMENTS_PER_DAY=5`：单患者单日挂号上限，防 Agent 失控循环或恶意囤号。
- 并发原子占位：`UPDATE doctor_schedules SET booked_slots = booked_slots + 1
  WHERE id=:id AND booked_slots < total_slots`，校验 `rowcount`，杜绝超卖。
- 审批载荷现在携带完整参数，医生能看到「结算哪笔预约」，盲批问题已解决。

**由此暴露并修正的一处语义问题**

评测中 `booking_settle_reject` 原断言「拒绝后不产生新预约」在第一轮锁号已执行的前提下
不成立。修正为更准确的语义：**拒绝的是医保结算而非挂号本身**——患者仍持号但需自费。
断言改为「不得出现已结算的新预约」。这也反映真实业务流程。

> 若后续产品决策改为「锁号也需审批」，需注意 `medicare_settle` 依赖 `lock_appointment`
> 的返回值填 `appointment_id`；当前多轮 ReAct 顺序（查号 → 锁号 → 结算）确保该依赖成立，
> 若两者同时进入待审批队列则结算将拿不到真实预约号。

---

## 六、顺带修复的工程问题

1. **FakeLLM 多轮 ReAct 顺序**：原先一次性生成该命名空间全部工具调用，导致
   `medicare_settle` 只能拿到硬编码占位符 `APT-1001`。改为按依赖分轮生成
   （`_DEFERRED_TOOLS`），并从上一轮工具结果中解析真实 `appointment_id`——
   复现真实模型的填参链路，也让归属校验能被端到端验证。
2. **`redline` 评测判定**：原以 `intent == "emergency"` 判定红线命中，但架构早已改为
   红线命中后走 `triage`（emergency agent 的 token 会被网关过滤导致空响应）。
   修正为以 `redline_reason` 非空判定 —— 红线用例从 2/5 恢复到 5/5。
3. **评测 hermetic 化**：`eval_offline.py` 默认强制 `LLM_MODE=fake`（结果确定性、CI 无密钥、
   不烧 API），需真实模型时用 `--real-model`；每次 e2e 前清空 `appointments` 与
   `idempotency_keys`，避免跨轮累积让幂等键/单日上限干扰结论。
4. **重复字典键（ruff F601）**：`tools/triage.py` 别名表中 6 处重复定义
   （如「打喷嚏」先映射「感冒」再被「鼻炎」静默覆盖）。保留当前生效值、删除被覆盖的冗余定义，
   消除「前一处意图无声失效」的隐患。
5. **ruff 全量清理**：`check` 与 `format` 从 27 处告警降到 0，CI 门禁真正生效。

---

## 七、验证结果

```
pytest                     80 passed   （原 58 + 安全回归 22）
ruff check .               All checks passed!
ruff format --check        49 files already formatted
scripts/eval_offline.py    红线 5/5 · 意图 5/5 · 端到端闭环 7/7 → 全部通过 ✅
前端 JS 语法                3 个 script 块 node --check 全通过
CSP 运行时校验              nonce 注入成功，页面无内联事件残留
```

关键 PoC 复验（均已从「可利用」变为「被拒绝」）：

| PoC | 整改前 | 整改后 |
| --- | --- | --- |
| 注册 `role=doctor` | 200 获得医护权限 | 422 |
| `read_lab_report("bob")` | 返回 bob 全部检验报告 | `PermissionError` |
| 结算他人预约 | `medicare_settled=True` | 「预约不存在或不属于当前患者」 |
| 跨患者会话 | 读到 bob 历史含报告全文 | 线程隔离，alice 线程无 bob 内容 |
| 伪造 XFF 绕过限流 | 30 次请求限流 0 次 | 出现 429 |
| 模型输出诊断/处方 | 直接推送患者 | `safety_override` 拦截 |

---

## 八、剩余风险与后续建议

已修复项覆盖了可远程利用的越权与合规红线，以下属**架构演进**范畴，建议后续排期：

1. **密钥管理**：`JWT_SECRET` / `ADMIN_API_KEY` 目前走环境变量，生产应接 KMS
   （阿里云 KMS / HashiCorp Vault）并支持定期轮转。
2. **医护 MFA**：高权限角色应强制二次因子（TOTP 或短信）。
3. **审计防篡改**：审计表应 append-only（DB 层回收写权限）+ 定期归档至 WORM 存储。
4. **WAF / DDoS**：网关层限流为单进程内存实现，多实例部署需换 Redis 集中限流。
5. **合规认证**：等保三级、NMPA SaMD（若涉及辅助诊断）、患者数据跨境评估。
6. **内容安全**：输出护栏目前是确定性正则，可叠加「LLM-as-judge」二次审核提升召回。

---

## 九、一句话总结

> 本次整改的核心不是「补几个漏洞」，而是**把信任边界从网关层下沉到数据访问层**：
> 让患者身份成为不可伪造的运行时上下文，而不是模型可以操纵的工具参数。
> 纵向防御（硬闸、JWT、限流）原本就扎实，现在横向隔离（对象级授权）与
> 可追责性（审批人落库）也已补齐——这是从「优秀 Demo」跨到「可过合规评审」的关键一步。

# 医疗诊疗 Agent · 安全性加固说明（P0/P1/P2 之后）

> 适用版本：在原有「P0 安全卫生 + P1 兜底容错 + P2 可观测」与「Tier-0 生命安全红线」基础上，
> 本次补齐**认证与前端安全**这一核心短板，使项目达到可对外小范围试点的认证安全基线。
> 验证：pytest **42 passed**（含 9 项新增安全单测）+ 真实 Postgres 端到端验证全部通过。

---

## 一、本次落地的安全能力（已验证）

### 1. 密码 hashing 强度 + 透明升级
- 方案：`PBKDF2-HMAC-SHA256`，轮数默认 **60 万**（可配 `PBKDF2_ROUNDS`），带方案版本号 `pbkdf2_sha256$v2$<轮数>$<salt>$<digest>`。
- 兼容历史 3 段哈希格式（`pbkdf2_sha256$<salt>$<digest>`）：存量账号登录时**自动重哈希为新格式**，无感升级、不强制改密。
- 校验用「存储轮数」回算，`hmac.compare_digest` 防时序攻击。

### 2. 短时效访问令牌 + 可吊销刷新令牌（Refresh Token Rotation）
- **访问令牌 15 分钟**（`JWT_ACCESS_EXP_MINUTES`），含 `jti / iss / nbf / type / tv` 声明，类型强校验（access/refresh 不可混用）。
- **刷新令牌 7 天**（`JWT_REFRESH_EXP_DAYS`），仅存 **SHA-256 哈希**于 `refresh_tokens` 表（拖库也无法还原原值）。
- 刷新即旋转：用旧刷新令牌换新令牌对后，**旧刷新令牌立即作废**（单次使用）。
- 前端 `apiFetch` 在访问令牌 401 时**自动用刷新令牌续期一次**，对用户体验无感。

### 3. 全局一键吊销（登出 / 改密即时生效）
- 用户表新增 `token_version` 字段；访问令牌内置 `tv` 声明。
- `GET /api/logout` 与改密路径 `bump_token_version` → 该用户**所有已发访问令牌立即失效**（跨进程/重启有效，不依赖内存）。
- 实测：登出后旧访问令牌 → `401`。

### 4. 账号防爆破锁定
- 连续失败达阈值（`ACCOUNT_LOCKOUT_ATTEMPTS=5`）→ 锁定 `ACCOUNT_LOCKOUT_MINUTES=15` 分钟，期间任何登录（含正确密码）返回 **423 Locked**。
- 成功登录清零失败计数；与 IP 级限流（`/auth/login` 10/60s）形成纵深。

### 5. 前端 XSS 收口
- 数据面板（号源/报告/体征/随访/科室/流程单）与审批横幅中**所有动态字段统一经 `esc()` 转义**。
- 审批横幅的 `_aid`、预约成功卡片的 **LLM 回复文本**（曾被 `innerHTML` 直接注入）现已转义。
- 报告/科室「智能提问」按钮改为 `data-*` 属性 + 事件委托，杜绝把用户输入拼进 `onclick` 字符串。

### 6. 统一安全响应头（纵深防御）
- `Content-Security-Policy`：默认仅同源，阻断外域脚本/资源（`script-src 'self' 'unsafe-inline'` 因当前为单文件内联脚本暂留，演进见第四节）。
- `X-Frame-Options: DENY` + CSP `frame-ancestors 'none'`：防点击劫持。
- `X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer`、`Permissions-Policy` 最小化、`Strict-Transport-Security`（TLS 启用后生效）。

---

## 二、端到端验证结果（:8080 真实 Postgres）

| 验证项 | 结果 |
|---|---|
| 登录返回 访问+刷新 令牌对 | ✅ `access_token` / `refresh_token` / `expires_in=900` |
| 访问令牌可用 | ✅ `/api/consent/status` → 200 |
| 刷新令牌旋转（旧刷新立即失效） | ✅ 旋转成功；复用旧刷新 → 401 |
| 登出全局吊销 | ✅ 旧访问令牌 → 401 |
| 连续失败 5 次锁定 | ✅ 第 5 次 → 423 |
| 安全响应头 | ✅ CSP / X-Frame-Options / nosniff / Referrer-Policy 全部下发 |

---

## 三、新增 / 变更清单

| 文件 | 变更 |
|---|---|
| `src/auth.py` | 重写：密码版本化+兼容旧格式、JWT 短效+刷新、认证结果含锁定状态、刷新令牌持久化/消费/吊销、`token_version` 吊销 |
| `src/config.py` | 新增 `JWT_ACCESS_EXP_MINUTES / JWT_REFRESH_EXP_DAYS / JWT_ISSUER / ACCOUNT_LOCKOUT_* / PBKDF2_ROUNDS` |
| `src/db.py` | `User/Doctor` 增 `token_version / failed_attempts / locked_until`；新增 `RefreshToken` 表 |
| `alembic/versions/d4e5f6a7b8c9_auth_hardening.py` | 迁移（**可回滚**：drop 表 + drop 列） |
| `src/gateway.py` | `SecurityHeadersMiddleware`；`/auth/login` 返令牌对；新增 `/auth/refresh`、`/auth/logout`；`RefreshRequest` 模型 |
| `client/chat.html` | 全量 `esc()` 转义 + 刷新令牌持久化 + `apiFetch`(401 自动续期) |
| `tests/test_auth.py` | 9 项安全单测（哈希/锁定/吊销/刷新/响应头） |
| `.env.example` | 文档化上述安全配置项 + TLS 部署提示 |

---

## 四、仍属「未闭环」的高优先级项（对外正式上线前必做）

> 以下为**认证安全已达标后，医疗合规纵深**的下一步；多数涉及基础设施与法务，需对应责任人签字。

1. **传输加密（红线）**：当前仍为明文 HTTP。生产必须前置 Nginx/Caddy 终止 **TLS 1.3 + HSTS + WAF**；证书私钥走 KMS，不落 `.env`。
2. **CSP 严格化**：将前端 JS 抽为外部文件 + `nonce`，移除 `script-src 'unsafe-inline'`，彻底阻断 XSS 注入脚本执行。
3. **PHI 加密与留存**：Postgres TDE / 字段级加密；`ChatLog` 患者输入属 PHI，需脱敏、留存期与最小化采集策略。
4. **医护 MFA + 权限细化**：医护登录强制多因子；敏感操作（开单/医保结算）加幂等/防重放。
5. **密钥与机密管理**：JWT 密钥、DB 口令、第三方 API Key 全部走密钥管理服务，禁止明文配置文件。
6. **依赖与镜像安全**：SCA（CVE）扫描、基础镜像漏洞扫描、CI 门禁。
7. **审计防篡改**：操作审计 append-only / WORM，明确「谁何时改了什么」。
8. **合规资质**：等保三级备案测评、NMPA SaMD 注册（若涉及诊断）、PIPL 数据合规评估、医疗责任险。

---

## 五、面试谈资（可一句话讲清楚）

- 「我们把认证从『一个永久 JWT』升级成**短时效访问令牌 + 可吊销刷新令牌 + token_version 全局吊销**，并用 `token_version` 实现登出/改密即时失效，比单纯刷新令牌更彻底。」
- 「密码哈希从 10 万轮升到 60 万轮 PBKDF2 并带版本号，**兼容历史哈希、登录时透明升级**，避免强制全量改密。」
- 「加了**账号锁定 + IP 限流**双重防爆破，以及**统一安全响应头 + 前端全量 XSS 转义**。」
- 「所有 schema 变更走 **Alembic 版本化且可回滚**，安全头/锁定/吊销都写了自动化测试，pytest 42 passed。」
